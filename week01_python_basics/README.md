# W1 学习日程（Python 基础语法）

## 零、排法：按「块」不按「天」（2026-09-24 改版）

**为什么改**：09-15 → 09-24 这 10 天里，实际开工 5 晚、未开工 5 晚（加班 ×2、出差 ×1、临时任务 ×1、要早起 ×1）。
出勤率约 **50%**——这不是意志力问题，是「每晚都有 45–60 分钟」这个前提本身不成立。
天天改日期既费事，又容易让人产生"我一直在落后"的错觉。所以改的是**排法**，不是目标。

**新规则三条：**

1. **单位是块，不是天。** 1 块 = 一晚能做完的量（约 2 节讲解，或一组练习）。表里只写「还剩几块」，不再绑死日期。
2. **每周基准 3 块。** 做到 4 块算赚到，只做 2 块也不叫欠账。撞上出差/加班就走下面的「10 分钟维持包」。
3. **进度看剩余块，不看日历。** 往后挪不再是"顺延"——本来就是按这个节奏排的。

**当前剩余：9 块**（D5 四 ＋ D6 三 ＋ D7 二）。D4 已于 **09-25 完成**（3/3）。
按每周 3 块算 ≈ 3 周，W1 大致 **10 月中旬**收尾；若每周能到 4 块，则 10 月上旬。
这不叫拖，叫把计划改成跟现实对得上。

> ↓ 下面是旧规则（逐日顺延）的历史记录，只作来时路的凭证，2026-09-24 起不再使用。

> 起始日调整：原定周一晚开跑，实际从 **2026-09-15（周二）晚**开始。
> 周投入目标 8–10 小时，拆成「平日每晚 45–60 分钟 + 周末各 2 小时」。
> 顺延记录一：**09-17（周四）加班**，当晚未开工 → D3 起整体后移一天。
> 顺延记录二：**09-19（周六）晚上 23:20 临时来工作**，D3 只读到讲解区【3】，练习没开始 →
>   明天（09-20）接着把 D3 读完 + 做完 5 题，D4 及以后再后移一天。
>   **→ 09-20 晚已完成**：5 题全对并 commit，D4 起按 09-21 / 09-22 / 09-23 / 09-24 正常推进。
> 顺延记录三：**09-21（周一）加班**，当晚未开工 → D4 起整体后移一天，W1 收尾日改为 09-25（周五）。
> 顺延记录四：**09-22（周二）要早起**，21:57 才开工、22:32 收工，讲解区读到【2】，练习未开始 →
>   明晚（09-23）接着把 D4 读完 + 做完 4 题，D5 起整体后移一天，W1 收尾日改为 09-26（周六）。
> 顺延记录五：**09-23（周三）忙**，讲解区读到【4】（第 3、4 节已逐句讲通、6 组对照实验已实跑验证），
>   练习未开始 → 明晚（09-24）读【5】【6】+ 做完 4 题再 commit，D5 起整体后移一天，
>   W1 收尾日改为 09-27（周日）。
> 顺延记录六：**09-24（周四）出差回来**，当晚休息、未开工 → D4 再续移到 09-25（周五），
>   D5 及以后各后移一天，W1 收尾日改为 **09-28（周一）**。
> 这不叫欠账，叫按实际节奏走。后面再撞上加班夜，同样规则再顺延一次即可。
> **复习用**：已学的 D1–D3 已整理成 `cheatsheet_D1-D3.md`（速查卡，含易混对照、踩坑清单、默写自测），随时翻，不必重读原文件。

## 一、进度表：看还剩几块，不看日历

| 模块 | 文件 | 主题 | 块 | 状态 |
|------|------|------|:--:|------|
| D1 | `01_basics.py` | 变量 / 字符串 / 数字 / 输入输出 | 2/2 | 已完成（09-15） |
| D2 | `02_data_structures.py` | 列表 / 元组 / 字典 / 集合 | 2/2 | 已完成（09-16） |
| D3 | `03_control_flow.py` | if / for / while / 异常处理 | 3/3 | 已完成（09-19 + 09-20） |
| D4 | `04_functions.py` | 函数 / 参数 / 模块导入 | 3/3 | 已完成（09-25，复习 +【5】【6】+ 4 题） |
| D5 | `05_file_io.py` | 文件读写 / CSV / JSON | 0/4 | 未开始 |
| D6 | `06_mini_project.py` | 小项目：政策清单统计分析 | 0/3 | 未开始 |
| D7 | `07_self_check.py` | 自测 8 题 + 复盘写笔记 | 0/2 | 未开始 |

**每块怎么切**（照这个切，一晚上做不完一整份是正常的）：

- D5 四块：①【0】【1】路径与读文本 ②【2】读 CSV（DictReader，重点）③【3】【4】写文本与写 JSON ④ 4 题练习
- D6 三块：① 主脚本跑通、看懂报告 ② 改造任务 1–2 ③ 改造任务 3 + 自查
- D7 两块：① 8 题自测 ② 写 `notes/W1.md` 的复盘

> 状态好就连推两块，没上限；只剩 20 分钟就做半块——**块是刻度，不是任务量下限**。
> 合计剩余 **9 块**，按每周 3 块 ≈ 3 周（W1 约 10 月中旬收尾）。这个数字就是计划本身，不是落后。

## 一·五、09-25（周五）当晚顺序：先复习，再往下读

**今晚的起点（一句话）**：还是 `04_functions.py` 这同一个文件，从【5】开始，也就是 **第 107 行**。
不是新文件、不是新的一天的新任务——09-23 读到【4】（第 101 行）为止，09-24 出差没开工，
今晚就接着往下。计划改的是"按块算进度"，不影响从哪一行开始。

> 快速定位：VS Code 打开 `04_functions.py` → 按 `Ctrl+G` → 输入 `107` → 回车。
>
> **（本段已于 09-25 当晚完成，D4 整块收尾。下一块从 `05_file_io.py` 开始，见第一节 D5 的切法。）**

**第 1 段 · 复习（15–20 分钟，别超时）**

| 看什么 | 怎么看 |
|--------|--------|
| `cheatsheet_D1-D3.md` | 只翻「易混对照 + 踩坑清单」，能说出每条讲的是什么就过，不重读原文件 |
| `04_functions.py` 的【1】【2】 | 合上屏幕默念：函数三要素是什么？默认参数该排在哪？（说不出再回看） |
| `04_functions.py` 的【3】【4】 | 只看两个差别：① 返回多个值靠什么接住；② 早返回和赋值的区别 |

> 复习的目的不是"再学一遍"，是把上周的 `return`/`.get`/切片这三条教训重新激活一下，
> 免得今晚写练习题时又踩同一个坑。**超过 20 分钟就停**，复习不是当天的主菜。

**第 2 段 · 往下读（20–30 分钟）**

- 【5】作用域：为什么函数里的变量外面拿不到，以及 `global` 为什么不建议用。
- 【6】主程序入口 `if __name__ == "__main__"`：这四行在防什么。

**第 3 段 · 做完 4 题 + commit**

- 4 题写在「你的代码 ↓」下面，写完再滑到最底部对答案。
- 全部跑通后按第五节的 4 步推 GitHub。

## 一·六、出差 / 加班夜：10 分钟维持包

**为什么要有这一节**：09-24 出差回来就是现成的例子——隔了几天不碰，前面的 `return`、`.get`、切片全淡了，
明晚开工得先花 15–20 分钟复习才能接上。隔得越久，接回去的成本越高。所以中断夜不做整块，只做"维持"。

**当晚任选一项，做完就算数（10 分钟封顶）：**

| 选项 | 做什么 | 用在哪 |
|------|--------|--------|
| A | 打开 `cheatsheet_D1-D3.md`，只读「踩坑清单」一段 | 手机/酒店都能看 |
| B | 不看文件，手写一个函数：`def format_amount(amount): return f"{amount/10000:.1f} 万元"` | 默写比阅读记得住 |
| C | 翻 `notes/W1.md` 的报错记录本，挑 3 条用自己的话解释一遍 | 周末或长途路上 |

**配套三条：**

1. **出差前**：把当天的练习 commit 掉再走（本地未提交最容易忘，也可能是唯一一份）。
2. **回来第一晚**：不要直接开新块，先复习 15–20 分钟再往下读（明晚 09-25 就是这么排的）。
3. **什么都不做也没关系**。维持包是给"想摸一下但没整块时间"用的，不是任务；不做也不记欠账。

## 二、每天怎么用这些文件（三步）

每个 `.py` 文件都是「可运行 + 带讲解 + 带练习」的三合一，用法固定：

1. **读**：从上往下读，注释里会写「应该输出什么」。
2. **跑**：按 `Ctrl+S` 保存 → 点 VS Code 右上角 ▶ 运行 → 看下方终端输出。
3. **练**：翻到文件下半部分的「动手练习」，自己写；写完再往下滑看参考答案。

> 参考答案在每个文件的最底部，**别提前滑**，先自己写错一遍比看答案学得快。

## 三、读不懂一行代码时怎么办：三级兜底

**先记住一条通用规则：带括号的是「动作」，不带括号的是「名字」。**

- 动作：`print(...)`、`len(...)`、`int(...)`、`replace(...)` —— 它们在"做一件事"
- 名字：`title`、`project_name`、`amount_str` —— 它们只是"装东西的盒子"

认得出这两类，一行代码就能读出大半意思。函数名到底是干什么的，属于**生词**，不是语法——查一次、见三次就记住了，不需要提前背单词表。

遇到看不懂的行，**不要卡住**，按顺序退：

| 情况 | 做什么 |
|------|--------|
| ① 注释看懂了 | 看代码怎么实现它 → 过 |
| ② 代码看不懂 | `Ctrl+S` 运行，对照注释里写的「应该输出什么」→ 对上了就过 |
| ③ 跑完还不明白 | 把这行连同上一行发我，逐词翻译 |

> **「看不懂但跑通了」是允许的，也是正常的。** 读代码是建立手感，不是逐字理解。
> 真正的理解发生在**写练习**的时候——写的时候会反复回去看上面的例子，那一刻才是学会的时刻。
> 判断「可以往下走」的标准不是能背出语法，而是**能看着一行代码说出它会输出什么**。

## 四、打卡表（做完打勾）

> 09-25 起改为**按块记**：备注里写清推进了第几块；总进度以第一节的「块」列为准，不再数天数。
> 出差/加班夜只做了维持包的，备注写「维持包」即可，不算中断也不占块数。

| 日期 | 文件 | 已读 | 已跑通 | 练习已完成 | 已 commit | 备注 |
|------|------|:---:|:---:|:---:|:---:|------|
| 09-15 | `01_basics.py` | ☑ | ☑ | ☑ | ☑ | 5 题全对；当晚 5 个卡点已记入 `notes/W1.md` |
| 09-16 | `02_data_structures.py` | ☑ | ☑ | ☑ | ☑ | 5 题全对；insert 位置语义 2 个卡点已记入 `notes/W1.md` |
| 09-17 | — | — | — | — | — | 加班，未开工（按顺延原则整体后移，不计欠账） |
| 09-18 | — | — | — | — | — | 顺延（未开工，D3 移到 09-19） |
| 09-19 | `03_control_flow.py` | 半 | ☑ | ☐ | ☐ | 夜里有工作插入，23:20 暂停：讲解区读到【3】，练习未开始，明天续 |
| 09-20 | `03_control_flow.py`（续） | ☑ | ☑ | ☑ | ☑ | 5 题全对；【5】首轮漏建列表已补；3 个卡点已记入 `notes/W1.md` |
| 09-21 | — | — | — | — | — | 加班，未开工（按顺延原则整体后移，不计欠账） |
| 09-22 | `04_functions.py` | 半 | ☑ | ☐ | ☐ | 要早起，35 min 收工：讲解读到【2】，练习未开始，明晚续 |
| 09-23 | `04_functions.py`（续） | 半 | ☑ | ☐ | ☐ | 忙，讲解读到【4】；第 3、4 节逐句讲通 + 6 组对照实验已实跑验证；练习未开始，明晚续 |
| 09-24 | — | — | — | — | — | 出差回来，休息，未开工（按顺延原则整体后移，不计欠账） |
| 09-25 | `04_functions.py`（再续） | ☑ | ☑ | ☑ | ☑ | D4 收尾：复习 → 读【5】【6】→ 4 题完成；【3】return 缩进 bug 已修；138 行注释「四行」改「两行」 |
| 待排 | `05_file_io.py` | ☐ | ☐ | ☐ | ☐ | D5 共 4 块，做完一格在备注写「第 X 块」 |
| 待排 | `06_mini_project.py` | ☐ | ☐ | ☐ | ☐ | D6 共 3 块 |
| 待排 | `07_self_check.py` | ☐ | ☐ | ☐ | ☐ | D7 共 2 块 |

> 旧规划曾把这三行写成 09-26 / 09-27 / 09-28 三个固定日期，已作废。日期列改成「待排」：
> 哪天开工就哪天填，不填也不算落后。

## 五、每天结束后把代码推到 GitHub（4 步，1 分钟）

1. VS Code 左侧点「源代码管理」图标（分叉那个）。
2. 在「更改」里点文件旁的 `+`，把要提交的文件暂存。
3. 上面的输入框写一句提交信息，例如：`feat: W1D1 Python 基础语法练习`。
4. 点 ✓ 提交，再点「同步更改」（↗）推送到 GitHub。

## 六、节奏原则（重要，别硬扛）

出差、加班、临时任务都会来，它们不是意外，是这个岗位的常态。所以规则从"顺延"改成"按块推进"：

- **不攒账，也不赶账。** 一周做到 3 块是基准，做到 4 块算赚到，只做 2 块也不记欠账。
- **不一次吃两块新内容。** 撞上中断就停在当前块，下次从块的开头继续，**不跳过、不合并**。
- **中断夜走维持包**（见一·六），回来第一晚先复习再往下读。
- **唯一不能妥协的**：一块里的练习必须写完再 commit，不要「读一遍就提交」。
- **允许慢，不允许断太久。** 隔一周以上完全不碰，比每晚只学 20 分钟损失大得多。

## 七、文件说明

| 文件/目录 | 说明 |
|-----------|------|
| `hello.py` | Day 0 已完成，环境验证脚本 |
| `01_basics.py` ~ `07_self_check.py` | 按上面日程依次打开 |
| `cheatsheet_D1-D3.md` | **D1–D3 速查卡**（复习用，3 / 15 / 30 分钟三种用法，含踩坑清单与默写自测） |
| `data/教育政策清单_示例.csv` | D5 / D6 要读的数据（示例数据，仅供练习） |
| `output/` | D6 小项目的输出目录，脚本自动创建 |

> 数据说明：`data/` 下的政策清单为**练习用示例数据**，条目信息未逐条核校，如需引用请以教育部官网原文为准。
